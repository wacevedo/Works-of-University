CREATE TABLE CINE (ID_CINE NUMBER NOT NULL, RNC NUMBER(9) NOT NULL, NOMBRE VARCHAR(50) NOT NULL, TEL NUMBER(10) NOT NULL, DIRECCION VARCHAR2(150) NOT NULL, ID_CIUDAD NUMBER NOT NULL);

CREATE TABLE SALAS (ID_SALA NUMBER NOT NULL, NOMBRE VARCHAR2(10) NOT NULL, CAPACIDAD NUMBER NOT NULL, HORE_APERTURA NUMBER NOT NULL, TIEMPO_DESCANSO_SESSIONES NUMBER, DISPOSICION_3D CHAR(2) NOT NULL,
ID_CINE NUMBER NOT NULL);

CREATE TABLE PELICULAS (ID_PELICULA NUMBER NOT NULL, TITULO VARCHAR2(30) NOT NULL, DIRECTOR VARCHAR2(50) NOT NULL, DURACION NUMBER NOT NULL, AÑO NUMBER(4) NOT NULL, EDAD_RECOMENDADA NUMBER(3) NOT NULL, 
GENERO VARCHAR2(30) NOT NULL, PAIS_PRODUCCION VARCHAR2(50) NOT NULL, DISTRIBUIDORA VARCHAR(50) NOT NULL, DIGITAL CHAR(2), DISPOSICION_3D CHAR(2), DESCRIPCION_CONTENIDO VARCHAR(100) NOT NULL, FECHA_EMISION DATE NOT NULL, 
EN_DIRECTO CHAR(2) NOT NULL);

CREATE TABLE EVENTOS (ID_EVENTOS NUMBER NOT NULL, CINE_PROGRAMADO NUMBER NOT NULL, SALA_PROGRAMADA NUMBER NOT NULL, CONTENIDO_PROGRAMADO number NOT NULL, FECHA_HORA DATE NOT NULL,
QUIEN_PROGRAMO VARCHAR2(30) NOT NULL, ID_SALA NUMBER NOT NULL);

CREATE TABLE PROVINCIAS (ID_PROVINCIA NUMBER NOT NULL, NOMBRE_PROV VARCHAR2(50) NOT NULL);

CREATE TABLE CIUDADES (ID_CIUDAD NUMBER NOT NULL, NOMBRE_CIUDAD VARCHAR2(50) NOT NULL, ID_PROVINCIA NUMBER NOT NULL);

       

ALTER TABLE CINE ADD CONSTRAINT PK_CINE PRIMARY KEY (ID_CINE);
ALTER TABLE SALAS ADD CONSTRAINT PK_SALAS PRIMARY KEY (ID_SALA);
ALTER TABLE PELICULAS ADD CONSTRAINT PK_PELICULAS PRIMARY KEY (ID_PELICULA);
ALTER TABLE EVENTOS ADD CONSTRAINT PK_EVENTOS PRIMARY KEY (ID_EVENTOS);
ALTER TABLE PROVINCIAS ADD CONSTRAINT PK_PROVINCIAS PRIMARY KEY (ID_PROVINCIA);
ALTER TABLE CIUDADES ADD CONSTRAINT PK_CIUDADES PRIMARY KEY (ID_CIUDAD);


ALTER TABLE CINE ADD CONSTRAINT FK_CIUDAD FOREIGN KEY (ID_CIUDAD) references CIUDADES (ID_CIUDAD);
ALTER TABLE SALAS ADD CONSTRAINT FK_CINE FOREIGN KEY (ID_CINE) references CINE (ID_CINE);
ALTER TABLE EVENTOS ADD CONSTRAINT FK_SALA FOREIGN KEY (SALA_PROGRAMADA) references SALAS (ID_SALA);
ALTER TABLE EVENTOS ADD CONSTRAINT FK_PELICULAS FOREIGN KEY (CONTENIDO_PROGRAMADO) references PELICULAS (ID_PELICULA);
ALTER TABLE CIUDADES ADD CONSTRAINT FK_PROVINCIA FOREIGN KEY (ID_PROVINCIA) references PROVINCIAS (ID_PROVINCIA);